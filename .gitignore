
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
/node_modules
/.pnp
.pnp.js
.pnp.loader.mjs

# Next.js build output
/.next/
/out/

# Production build
/build

# Static HTML exports
/dist

# Local environment variables
.env*.local
.env.development.local
.env.test.local
.env.production.local
.env

# IDE and editor specific
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace
.idea/

# macOS specific
.DS_Store
._*
.Spotlight-V100
.Trashes

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Optional: Test files coverage
# coverage/
# .nyc_output

# Optional: Storybook
# .storybook
# storybook-static

# Cache
.npm/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz

# SQLite Database files (if you want to ensure local dev dbs are never committed)
# This is especially important if your local dev DB might have a different name
# or if you experiment with DB files directly in the project root.
*.sqlite
*.sqlite3
*.sqlite-journal
*.sqlite3-journal

# If your data directory for local dev is inside the project (it shouldn't be for production)
# /data/ <- We already added this for the server, but good to have generally

# Terraform files
.terraform/
*.tfstate
*.tfstate.*.backup

# Miscellaneous
*.swp
*.swo
Thumbs.db
ehthumbs.db
Desktop.ini
cache/
